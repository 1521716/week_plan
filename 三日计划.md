# 51 单片机 ☀️ 三日计划 #1

> 周期：2026.07.13（晚） ~ 2026.07.16
> 主题：**串口通信（UART）攻克**
> 前置：定时器模块已收官、中断系统已预习

---

## 🎯 总目标

三天内从零掌握 UART 串口通信，最终实现 PC 端命令控制单片机外设。

**难度评级**：⭐⭐⭐（比定时器难，但比 I2C 简单——江协教程中最需要静心啃的模块）

---

## 📅 每日分解

### Day 1 — 07.13 晚（理论奠基）

> 串口是什么、怎么配、跑通第一个回显

| 任务 | 内容 | 预计产出 | 时长 |
|------|------|----------|------|
| 串口理论预习 | RS232 电平 vs TTL 电平、波特率概念、异步通信帧格式（起始位-数据位-停止位） | `串口通信_理论预习.md` | 1h |
| 寄存器走读 | SCON（工作方式）、PCON（波特率加倍）、SBUF（数据缓冲）、IE（中断使能） | 寄存器速查表 | 30min |
| 波特率计算 | 定时器1 溢出率 → 波特率公式推导 | 理解 9600bps 配置 | 20min |
| **回显实验** | PC 通过串口助手发字符 → MCU 收到后原样返回 | 跑通第一个串口程序 | 1h |
| 笔记归档 | 记录踩坑点、寄存器配置流程 | 更新理论笔记 | 20min |

**Day 1 检查点**：串口助手里输入 `A`，收到 `A` ✅

---

### Day 2 — 07.14（命令控制）

> 不再是"回声筒"，PC 发指令控制 MCU 外设

| 任务 | 内容 | 预计产出 | 时长 |
|------|------|----------|------|
| 字符串接收 | 从单字符回显 → 接收完整命令字符串 | 环形缓冲区/简易字符串处理 | 1h |
| 命令解析 | 定义命令协议（如 `LED:ON` / `LED:OFF` / `BEEP:500`） | 命令表 + 解析函数 | 40min |
| 外设联动 | LED 开关、蜂鸣器鸣叫、LCD 显示 PC 发来的消息 | 3~4 条可用命令 | 1.5h |
| 状态反馈 | 执行结果回传 PC（`OK` / `ERR:xxx`） | 双向通信闭环 | 30min |

**Day 2 检查点**：串口助手发 `LED:ON`，开发板 LED 亮 + PC 收到 `OK` ✅

---

### Day 3 — 07.15~07.16（应用 + 收尾）

> 用串口做点"实用的" + 扩展技能

| 任务 | 内容 | 预计产出 | 时长 |
|------|------|----------|------|
| **电子钟校准** | PC 发 `TIME:14:30:00` → MCU 更新简易时钟时间 | 串口同步 RTC 时间 | 1.5h |
| 定时器1 独立配置 | 不依赖 Timer0 代码模板，从零写 Timer1 初始化 | Timer1 配置能力 | 1h |
| 外部中断预习 | INT0/INT1 触发方式、寄存器、代码模板 | `外部中断_预习.md` | 40min |
| 三日总结 | 串口通信知识卡片：寄存器速查 + 常见坑 + 最佳实践 | 总结笔记 | 30min |

**Day 3 检查点**：串口助手发 `TIME:14:30:00`，LCD 时钟从 14:30:00 开始走 ✅

---

## 📊 进度追踪

| 日期 | 计划 | 实际完成 | 备注 |
|------|------|----------|------|
| 07.13 晚 | 理论 + 回显 | ⬜ | |
| 07.14 | 命令控制 | ⬜ | |
| 07.15 | 电子钟校准 | ⬜ | |
| 07.16 | 收尾 + 总结 | ⬜ | |

---

## 🔗 依赖关系

```
定时器1（波特率发生器）
    │
    ├── 串口中断（ES + EA）
    │       │
    │       ├── Day1: 回显（单字符收发）
    │       ├── Day2: 命令控制（字符串 + 解析）
    │       └── Day3: 时钟校准（应用层协议）
    │
定时器0（已完成）
    │
    └── 外部中断预习（Timer0 知识可迁移到 Timer1）
```

---

## ⚠️ 已知难点预警

| 坑 | 说明 | 对策 |
|----|------|------|
| 波特率不准 | 晶振 11.0592MHz vs 12MHz，后者波特率有误差 | 用 11.0592MHz（开发板默认） |
| 串口中断与 Timer1 冲突 | Timer1 被串口占用，不能再做普通定时 | Day3 单独练习 Timer1 的定时功能（临时切换模式） |
| 字符串接收边界 | 没有 `\0` 概念，需手动管理缓冲区 | 用固定长度或 `\r\n` 结束符 |
| RI/TI 混淆 | RI=接收完成，TI=发送完成，共用一个中断号 | 中断函数内用 `if (RI)` / `if (TI)` 区分 |

---

*创建于 2026-07-13 晚 | 三日计划 #1*
